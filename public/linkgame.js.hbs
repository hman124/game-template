function linkGame() {
  window["userId"] = "{{userId}}";
  window["gamePin"] = "{{gamePin}}";
  socket.on("connect", () => {
    socket.emit("linkGame", ["{{gamePin}}", "{{userId}}"]);
  });
}
linkGame();